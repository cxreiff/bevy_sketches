{
  "name": "bevy_sketches",
  "version": "0.0.1",
  "scripts": {
    "start": "concurrently 'rsw watch' 'vite'",
    "build": "rsw build && vite build",
    "rsw": "rsw"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "vite": "^4.3.9",
    "vite-plugin-rsw": "^2.0.11"
  }
}
